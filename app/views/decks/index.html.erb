<div class="container">
  <h1>Decks</h1>
  <% @decks.each do |deck| %>
    <div>
      <h2><%= link_to deck.title, deck_path(deck) %></h2>
      <p><%= deck.description %></p>
    </div>
  <% end %>
  <%= link_to 'New Deck', new_deck_path %>
</div>
